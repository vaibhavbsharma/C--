int a, b, c;

int foo(int f, int e) {
    return f + e;
}

int bar(int h, float g) {
    return h - g;
}

int main() {
    if (a) {
        write("hello parser");
    }

    if (b == 0) {
        write("b is zero");
    } else {
        write("b is not zero");
    }
}

